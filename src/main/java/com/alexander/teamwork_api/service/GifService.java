package com.alexander.teamwork_api.service;

import com.alexander.teamwork_api.dto.GifRequest;
import com.alexander.teamwork_api.dto.GifResponse;
import org.springframework.core.io.Resource;
import org.springframework.data.domain.Page;
import org.springframework.security.core.Authentication;

public interface GifService {
//    // Upload Gif
//    GifResponse uploadGif(GifRequest request, Authentication authentication);
//
//    // Return GIFs ordered from newest to oldest
//    Page<GifResponse> getAllGifs(int page, int size);
//
//    // Returns one GIF using its ID.
//    GifResponse getGifById(Long id);
//
//    // Return uploaded GIF file
//    Resource getGifFile(String fileName);
//
//    // Deletes a GIF if it belongs to the logged-in employee.
//    void deleteGif(Long id,Authentication authentication);


}
