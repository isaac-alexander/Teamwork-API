Add JWT Dependencies

<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-api</artifactId>
    <version>0.12.6</version>
</dependency>

<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-impl</artifactId>
    <version>0.12.6</version>
    <scope>runtime</scope>
</dependency>

<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-jackson</artifactId>
    <version>0.12.6</version>
    <scope>runtime</scope>
</dependency>


Create JwtService
add imports
Create secret key(String) for test

Why make the method static?
Because the mapper doesn't store any data.

@PrePersist and @PreUpdate
Instead of writing article.setCreatedAt(LocalDateTime.now());

inside every service method JPA does it automatically.

When you save a new article with

articleRepository.save(article);

Spring automatically calls prePersist();

When you edit an article
articleRepository.save(article);

Spring automatically calls preUpdate();

GifCommentRepository
GifCommentRequest
GifCommentResponse
GifCommentMapper
GifCommentService
GifCommentController